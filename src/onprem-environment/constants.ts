export const BUILDER_TARGET = {
  "builder": "ng-process-env:collectVars",
  "options": {
    "environmentFile": ""
  }
};

export const BUILDER_COMMAND = 'collect-vars';

export const DEFAULT_CONFIG_NAME = 'onprem';
